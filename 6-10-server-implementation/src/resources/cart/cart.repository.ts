import { CartEntity } from "../../types/cart.entity";

const carts: CartEntity[] = []; 

export const getCart = async (userId: string): Promise<CartEntity | null> => {
    return null;
}

export const putProductToCart = async (userId: string): Promise<CartEntity | null> => {
    return null;
}

